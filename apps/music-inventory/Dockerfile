# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @lexamica/music-inventory`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3002:3002 -t lexamica-music-inventory`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3002

WORKDIR /app

RUN addgroup --system lexamica-music-inventory && \
          adduser --system -G lexamica-music-inventory lexamica-music-inventory

COPY apps/music-inventory/dist lexamica-music-inventory/
COPY apps/music-inventory/package.json lexamica-music-inventory/
RUN chown -R lexamica-music-inventory:lexamica-music-inventory .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix lexamica-music-inventory --omit=dev -f install

CMD [ "node", "lexamica-music-inventory" ]
