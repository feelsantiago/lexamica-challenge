# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @lexamica/music-faker`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 5000:5000 -t lexamica-music-faker`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=5000

WORKDIR /app

RUN addgroup --system lexamica-music-faker && \
          adduser --system -G lexamica-music-faker lexamica-music-faker

COPY apps/music-faker/dist lexamica-music-faker/
COPY apps/music-faker/package.json lexamica-music-faker/
RUN chown -R lexamica-music-faker:lexamica-music-faker .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix lexamica-music-faker --omit=dev -f install

CMD [ "node", "lexamica-music-faker" ]
