# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @lexamica/music-auth`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3001:3001 -t lexamica-music-auth`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3001

WORKDIR /app

RUN addgroup --system lexamica-music-auth && \
          adduser --system -G lexamica-music-auth lexamica-music-auth

COPY apps/music-auth/dist lexamica-music-auth/
COPY apps/music-auth/package.json lexamica-music-auth/
RUN chown -R lexamica-music-auth:lexamica-music-auth .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix lexamica-music-auth --omit=dev -f install

CMD [ "node", "lexamica-music-auth" ]
