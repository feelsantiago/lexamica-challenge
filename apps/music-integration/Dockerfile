# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @lexamica/music-integration`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3003:3003 -t lexamica-music-integration`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3003

WORKDIR /app

RUN addgroup --system lexamica-music-integration && \
          adduser --system -G lexamica-music-integration lexamica-music-integration

COPY apps/music-integration/dist lexamica-music-integration/
COPY apps/music-integration/package.json lexamica-music-integration/
RUN chown -R lexamica-music-integration:lexamica-music-integration .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix lexamica-music-integration --omit=dev -f install

CMD [ "node", "lexamica-music-integration" ]
