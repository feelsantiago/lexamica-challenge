# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @lexamica/music-config`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t lexamica-music-config`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system lexamica-music-config && \
          adduser --system -G lexamica-music-config lexamica-music-config

COPY apps/music-config/dist lexamica-music-config/
COPY apps/music-config/package.json lexamica-music-config/
RUN chown -R lexamica-music-config:lexamica-music-config .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix lexamica-music-config --omit=dev -f install

CMD [ "node", "lexamica-music-config" ]
